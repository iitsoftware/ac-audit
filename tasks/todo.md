- [x] Inspect `views/change.ejs` for AC-Change import dialogs and existing form markup conventions.
- [x] Add associated labels for the task-list and risk-analysis file inputs.
- [x] Verify the template contains no remaining unlabeled file inputs.

## Review

- Added visible labels using `for` attributes matching each import input ID.
- Kept the change scoped to the AC-Change template; no JavaScript or styling changes were needed.
